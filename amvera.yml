---
meta:
  environment: jvm
  toolchain:
    name: maven
    version: 17
build:
  artifacts:
    target/*.jar: /
  args: springboot;repackage
run:
  persistenceMount: /data
  containerPort: 80
  jarName: checklist-project-0.0.1-SNAPSHOT.jar
