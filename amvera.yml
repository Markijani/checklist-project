---
meta:
  environment: jvm
  toolchain:
    name: maven
    version: 17
build:
  artifacts:
    checklist-project/target/*.jar: /
run:
  persistenceMount: /data
  containerPort: 8080
  jarName: checklist-project-0.0.1-SNAPSHOT.jar
